# TTS - 项目规格说明书 v2.0

## 1. 项目目标

**自己实现一个简化版 VITS 模型**，在 Mac 本地训练，并能合成中文/英文语音。

---

## 2. 技术架构

### 2.1 整体架构

```
文本输入
    ↓
文本分析（文本 → 音素/拼音）
    ↓
声学模型（VITS - 生成梅尔频谱）
    ↓
声码器（HiFi-GAN 简化版 - 梅尔频谱 → 波形）
    ↓
音频输出（.wav 文件）
```

### 2.2 为什么选择 VITS？

| 特点 | 说明 |
|------|------|
| 端到端 | 从文本直接到音频，减少模块间的误差 |
| 高质量 | 效果好，自然度高 |
| 开源参考多 | 有成熟 PyTorch 实现可参考，业界标准方案 |
| 可简化 | 层次/参数可以缩减，适合本地训练 |

> **注意**：VITS 看起来组件多，但简化后核心只有文本编码器、VAE 隐变量、解码器三部分。我们不重造轮子，而是参考开源实现、理解原理后做适度简化，降低实现门槛。

### 2.3 模型组成

```
VITS 模型（简化版）
├── 文本编码器 (Text Encoder)
│   └── 把文字转成数字特征
├── 解码器 (Decoder)
│   └── 生成梅尔频谱 (Mel-Spectrogram)
│
HiFi-GAN 声码器（简化版）
└── 把梅尔频谱转成声音波形
```

---

## 3. 模型实现（简化版）

### 3.1 VITS 主干网络（简化）

| 模块 | 原始 VITS | 简化版本 | 说明 |
|------|-----------|----------|------|
| 文本编码器 | 8 层 Transformer | 2 层 | 减少层数 |
| 隐变量维度 | 256 | 128 | 压缩维度 |
| 注意力层 | 12 | 4 | 减少计算量 |
| 说话人嵌入 | 可选 | 不需要 | 单人语音 |

**简化后的参数量**：约 **3-5M** 参数（vs 原始 200M+）

具体简化规格：

| 模块 | 原始 VITS | 简化版本 |
|------|-----------|----------|
| 文本编码器 | 8 层 Transformer，隐藏维度 256 | 2 层 Transformer，隐藏维度 192 |
| 隐变量维度 | 192 | 96（VAE bottleneck） |
| 解码器 | 12 层前馈网络 | 6 层前馈网络 |
| 时长预测器 | 6 层 | 2 层 |

### 3.2 HiFi-GAN 声码器（简化）

| 模块 | 原始 HiFi-GAN | 简化版本 |
|------|---------------|----------|
| 上采样层 | [8,8,2,2] 4层，总上采样×32 | [8,8,2,2] 4层，总上采样×32 |
| 残差块 | 每层 30 个卷积块 | 每层 3 个卷积块 |
| 参数量 | 13M | 约 **1-2M** |
| Mel 频谱维度 | 80 | 80（与 VITS 输出对齐）|

### 3.3 总模型大小

```
预计最终模型：10-15MB（可以接受）
```

---

## 4. 数据集

### 4.1 选择

| 数据集 | 语言 | 时长 | 说明 |
|--------|------|------|------|
| LJ Speech | 英文 | 24h | 经典 TTS 数据集，小巧 |
| AIShell-1 | 中文 | 178h | 中文语音数据集，较大 |

### 4.2 初期方案

先用 **LJ Speech 英文数据集**：
- 数据量小（24h），训练快
- 验证流程正确性
- 之后再迁移到中文

### 4.3 数据格式

```
wav/          # 音频文件
   ├── LJ001.wav
   └── LJ002.wav
metadata.csv  # 格式：wav_path|text
```

---

## 5. 训练方案（Mac 本地）

### 5.1 硬件加速

| 方式 | Mac 支持 | 说明 |
|------|----------|------|
| CPU | ✅ 总是可用 | 慢，不推荐 |
| MPS | ✅ Apple Metal | Mac M1/M2 专用，比 CPU 快 |
| CUDA | ❌ 不可用 | PC/Linux 用这个 |

### 5.2 训练配置（简化版）

| 参数 | 值 | 说明 |
|------|-----|------|
| batch_size | 8-16 | Mac 内存有限，不能太大 |
| learning_rate | 1e-4 | 标准学习率 |
| 训练轮次 | 100-200 | 监控 loss 下降 |
| 序列长度 | 256-512 | 控制显存使用 |
| 混合精度 | 可选 | 加快训练 |

### 5.3 预估时间（Mac M1/M2）

| 场景 | 时间 | 说明 |
|------|------|------|
| LJ Speech (24h) | **3-6 小时** | 可接受 |
| AIShell (178h) | **1-3 天** | 较长 |

### 5.4 训练流程

```
1. 数据预处理
   - 音频 → 梅尔频谱
   - 文本 → 音素
   - 打包成训练批次

2. 训练循环
   - 前向传播 → 计算 loss → 反向传播 → 更新权重

3. 验证
   - 每 N 轮用验证集评估
   - 保存最佳模型

4. 导出
   - 模型 → ONNX 或 TorchScript
   - 方便后续推理
```

---

## 6. 推理流程

### 6.1 推理架构

```
输入文本
    ↓
文本到音素（使用 gruut/pypinyin）
    ↓
VITS 推理（生成梅尔频谱）
    ↓
HiFi-GAN 推理（生成波形）
    ↓
输出 wav 数据
```

**文本转音素方案**：
- 英文：使用 `gruut` 或 `phonemizer`，将 "hello" → `HH AH L OW`
- 中文：使用 `pypinyin`，将 "你好" → `n i hao`
- 音素序列作为 VITS 输入，而非直接用汉字

**依赖安装**：
```bash
pip install gruut pypinyin
```

### 6.2 推理速度目标

| 输入长度 | 目标时间 |
|----------|----------|
| 20 字内 | < 1 秒 |
| 100 字内 | < 3 秒 |

---

## 7. 项目结构

```
TTS/
├── spec.md                  # 本规格文档
├── CLAUDE.md                # 项目指令
├── README.md                # 使用说明
│
├── docs/                    # 原理文档
│   ├── vits_architecture.md # VITS 模型原理
│   └── hifigan_architecture.md
│
├── data/                    # 数据目录
│   ├── raw/                # 原始数据
│   └── processed/          # 处理后的数据
│
├── src/                     # 源代码
│   ├── __init__.py
│   ├── dataset.py          # 数据集处理
│   ├── model/              # 模型定义
│   │   ├── vits.py         # VITS 模型
│   │   └── hifigan.py      # HiFi-GAN 声码器
│   ├── preprocess.py       # 数据预处理
│   ├── train.py            # 训练脚本
│   ├── infer.py            # 推理脚本
│   └── export.py           # 模型导出
│
├── models/                  # 保存的模型
│   ├── vits_final.pt
│   └── hifigan_final.pt
│
├── webui/                   # Web 界面
│   ├── index.html
│   └── style.css
│
├── server.py               # API 服务
├── requirements.txt
└── run.sh
```

---

## 8. 功能验收标准

### 8.1 模型训练验收

| # | 标准 | 验证方法 |
|---|------|----------|
| 1 | 训练脚本能在 Mac 上运行 | 无报错，能正常启动 |
| 2 | 训练集 loss 从初始值下降 ≥ 50% | 第 50 轮 loss / 第 1 轮 loss ≤ 0.5 |
| 3 | 验证集 loss 在连续 10 轮内无明显上升 | 无过拟合迹象 |
| 4 | 模型文件能保存 | `models/vits_final.pt` 文件存在 |
| 5 | 模型能加载 | 无报错，成功加载到内存 |

### 8.2 推理验收

| # | 标准 | 验证方法 |
|---|------|----------|
| 1 | 输入 "hello world" 有音频输出 | 听到声音，生成 .wav 文件 |
| 2 | 输出是有效 wav 文件 | 用播放器或 `wave` 库验证 |
| 3 | 音频时长与文本长度匹配 | 时长 / 字数 ≈ 0.2-0.4 秒/字 |
| 4 | 合成 20 字英文 < 2 秒 | Mac M1/M2 实测计时 |
| 5 | 合成音频 MOS 主观评分 ≥ 3.0 | 1-5 分制，人工评估 |

### 8.3 整体验收

| # | 标准 |
|---|------|
| 1 | Web 界面能正常打开 |
| 2 | 文本能转换为语音并播放 |
| 3 | 音频能下载保存 |

---

## 9. 调试与问题排查

| 症状 | 可能原因 | 解决方案 |
|------|----------|----------|
| loss 一直很高不下降 | 学习率过大或数据预处理错误 | 降低 lr，检查频谱是否正确生成 |
| 输出音频全是噪声 | 声码器未正确加载或梅尔频谱范围不对 | 验证梅尔频谱数值范围 |
| 模型推理很慢 | batch_size 太大或未做优化 | 减小 batch，使用 `torch.compile` |
| 训练显存 OOM | 序列长度太长 | 减少 max_len，降低 batch_size |
| 音频有明显的杂音/失真 | HiFi-GAN 训练不足 | 增加训练轮次，检查音频归一化 |
| 时长预测偏差大 | 时长预测器欠拟合 | 检查文本对齐数据是否正确 |

---

## 10. 技术依赖

### 10.1 Python 环境

| 依赖 | 版本 | 用途 |
|------|------|------|
| Python | >= 3.9 | 环境 |
| PyTorch | 2.0+ | 深度学习框架 |
| torchaudio | 2.0+ | 音频处理 |
| numpy | latest | 数值计算 |
| scipy | latest | 信号处理 |

### 10.2 辅助库

| 库 | 用途 |
|---|------|
| gruut / phonemizer | 文本转音素 |
| matplotlib | 可视化 |

---

## 11. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| Mac 训练太慢 | 无法完成 | 用最小模型 + 最小数据集验证 |
| 模型质量差 | 听起来不像 | 先用公开数据集 + 参考架构 |
| 内存不足 | 训练中断 | 减小 batch_size 和序列长度 |

---

## 12. 后续扩展（当前不实现）

- 流式推理（边生成边播放）
- 中文模型训练
- 多说话人支持
- 模型量化优化
- WebGPU 加速

---

## 13. 参考资料

- [VITS 论文](https://arxiv.org/abs/2106.06103) — 原始论文
- [VITS 官方 PyTorch 实现 (jaywalnut310)](https://github.com/jaywalnut310/vits) — **核心参考代码**
- [VITS 论文详解博客](https://jaywalnut310.github.io/vits-heading-page/) — 通俗原理讲解
- [HiFi-GAN 论文](https://arxiv.org/abs/2010.05646) — 声码器论文
- [HiFi-GAN 官方实现](https://github.com/jik876/hifi-gan) — 声码器参考代码
- [LJ Speech 数据集](https://keithito.com/LJ-Speech-Dataset/) — 英文训练数据（24h）
- [AIShell-1 数据集](https://www.openslr.org/33/) — 中文训练数据（178h）

---

*文档版本：v2.1*
*创建日期：2026/04/22*
*重大更新：从使用预训练模型 → 自主实现简化版 VITS*
*更新日期：2026/04/22*
*更新内容：具体化模型参数、量化验收标准、新增调试章节、更新参考资料*
